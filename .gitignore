# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Test coverage
coverage/

# Databases (runtime data, never committed)
*.db
*.db-journal
*.db-wal
*.db-shm

# Logs
*.log
logs/

# Runtime data directory (if project ever runs its own install inside the source tree)
.tasks/

# Env files
.env
.env.local
.env.*.local

# OS / editor
.DS_Store
.vscode/
.idea/

# Review artifacts — briefs and reviewer outputs stay local, never committed.
# They are useful during a review round but aren't history we need to carry.
reviewBrief*.md
codeReview*.md
securityReview*.md
