// Project Dispatcher — placeholder entry point.
//
// The real daemon entry point will live in src/daemon/index.ts once MVP-06
// (Fastify server skeleton) lands. This file exists so `npm run build`
// produces output and the package has a valid `main`/`bin` target.

export function main(): void {
  // Intentionally empty for now.
}
